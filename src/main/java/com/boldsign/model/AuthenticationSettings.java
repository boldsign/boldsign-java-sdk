/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * AuthenticationSettings
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class AuthenticationSettings {
  /**
   * Gets or Sets authenticationFrequency
   */
  @JsonAdapter(AuthenticationFrequencyEnum.Adapter.class)
  public enum AuthenticationFrequencyEnum {
    NONE("None"),
    
    EVERY_ACCESS("EveryAccess"),
    
    UNTIL_SIGN_COMPLETED("UntilSignCompleted"),
    
    ONCE_PER_DOCUMENT("OncePerDocument"),
    
    NULL("null");

    private String value;

    AuthenticationFrequencyEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static AuthenticationFrequencyEnum fromValue(String value) {
      for (AuthenticationFrequencyEnum b : AuthenticationFrequencyEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      return null;
    }

    public static class Adapter extends TypeAdapter<AuthenticationFrequencyEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final AuthenticationFrequencyEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public AuthenticationFrequencyEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return AuthenticationFrequencyEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      AuthenticationFrequencyEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_AUTHENTICATION_FREQUENCY = "authenticationFrequency";
  @SerializedName(SERIALIZED_NAME_AUTHENTICATION_FREQUENCY)
  private AuthenticationFrequencyEnum authenticationFrequency;

  public AuthenticationSettings() {
  }

  public AuthenticationSettings authenticationFrequency(AuthenticationFrequencyEnum authenticationFrequency) {
    this.authenticationFrequency = authenticationFrequency;
    return this;
  }

  /**
   * Get authenticationFrequency
   * @return authenticationFrequency
   */
  @javax.annotation.Nullable
  public AuthenticationFrequencyEnum getAuthenticationFrequency() {
    return authenticationFrequency;
  }

  public void setAuthenticationFrequency(AuthenticationFrequencyEnum authenticationFrequency) {
    this.authenticationFrequency = authenticationFrequency;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    AuthenticationSettings authenticationSettings = (AuthenticationSettings) o;
    return Objects.equals(this.authenticationFrequency, authenticationSettings.authenticationFrequency);
  }

  @Override
  public int hashCode() {
    return Objects.hash(authenticationFrequency);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class AuthenticationSettings {\n");
    sb.append("    authenticationFrequency: ").append(toIndentedString(authenticationFrequency)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (authenticationFrequency != null) {
        if (isFileTypeOrListOfFiles(authenticationFrequency)) {
            fileTypeFound = true;
        }

        if (authenticationFrequency.getClass().equals(java.io.File.class) ||
            authenticationFrequency.getClass().equals(Integer.class) ||
            authenticationFrequency.getClass().equals(String.class) ||
            authenticationFrequency.getClass().equals(java.net.URI.class)||
            authenticationFrequency.getClass().isEnum()) {
            map.put("authenticationFrequency", authenticationFrequency);
        } else if (isListOfFile(authenticationFrequency)) {
            for(int i = 0; i< getListSize(authenticationFrequency); i++) {
                map.put("authenticationFrequency", authenticationFrequency);
            }
        }
        else {
          map.put("authenticationFrequency", JSON.serialize(authenticationFrequency));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("authenticationFrequency");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to AuthenticationSettings
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!AuthenticationSettings.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in AuthenticationSettings is not found in the empty JSON string", AuthenticationSettings.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("authenticationFrequency") != null && !jsonObj.get("authenticationFrequency").isJsonNull()) && !jsonObj.get("authenticationFrequency").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `authenticationFrequency` to be a primitive type in the JSON string but got `%s`", jsonObj.get("authenticationFrequency").toString()));
      }
      // validate the optional field `authenticationFrequency`
      if (jsonObj.get("authenticationFrequency") != null && !jsonObj.get("authenticationFrequency").isJsonNull()) {
        AuthenticationFrequencyEnum.validateJsonElement(jsonObj.get("authenticationFrequency"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!AuthenticationSettings.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'AuthenticationSettings' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<AuthenticationSettings> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(AuthenticationSettings.class));

       return (TypeAdapter<T>) new TypeAdapter<AuthenticationSettings>() {
           @Override
           public void write(JsonWriter out, AuthenticationSettings value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public AuthenticationSettings read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of AuthenticationSettings given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of AuthenticationSettings
   * @throws IOException if the JSON string is invalid with respect to AuthenticationSettings
   */
  public static AuthenticationSettings fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, AuthenticationSettings.class);
  }

  /**
   * Convert an instance of AuthenticationSettings to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

