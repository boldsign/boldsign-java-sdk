/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * SignatureFrameSettings
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class SignatureFrameSettings {
  public static final String SERIALIZED_NAME_ENABLE_SIGNATURE_FRAME = "enableSignatureFrame";
  @SerializedName(SERIALIZED_NAME_ENABLE_SIGNATURE_FRAME)
  private Boolean enableSignatureFrame = false;

  public static final String SERIALIZED_NAME_SHOW_RECIPIENT_NAME = "showRecipientName";
  @SerializedName(SERIALIZED_NAME_SHOW_RECIPIENT_NAME)
  private Boolean showRecipientName = false;

  public static final String SERIALIZED_NAME_SHOW_RECIPIENT_EMAIL = "showRecipientEmail";
  @SerializedName(SERIALIZED_NAME_SHOW_RECIPIENT_EMAIL)
  private Boolean showRecipientEmail = false;

  public static final String SERIALIZED_NAME_SHOW_TIME_STAMP = "showTimeStamp";
  @SerializedName(SERIALIZED_NAME_SHOW_TIME_STAMP)
  private Boolean showTimeStamp = false;

  public SignatureFrameSettings() {
  }

  public SignatureFrameSettings enableSignatureFrame(Boolean enableSignatureFrame) {
    this.enableSignatureFrame = enableSignatureFrame;
    return this;
  }

  /**
   * Get enableSignatureFrame
   * @return enableSignatureFrame
   */
  @javax.annotation.Nullable
  public Boolean getEnableSignatureFrame() {
    return enableSignatureFrame;
  }

  public void setEnableSignatureFrame(Boolean enableSignatureFrame) {
    this.enableSignatureFrame = enableSignatureFrame;
  }


  public SignatureFrameSettings showRecipientName(Boolean showRecipientName) {
    this.showRecipientName = showRecipientName;
    return this;
  }

  /**
   * Get showRecipientName
   * @return showRecipientName
   */
  @javax.annotation.Nullable
  public Boolean getShowRecipientName() {
    return showRecipientName;
  }

  public void setShowRecipientName(Boolean showRecipientName) {
    this.showRecipientName = showRecipientName;
  }


  public SignatureFrameSettings showRecipientEmail(Boolean showRecipientEmail) {
    this.showRecipientEmail = showRecipientEmail;
    return this;
  }

  /**
   * Get showRecipientEmail
   * @return showRecipientEmail
   */
  @javax.annotation.Nullable
  public Boolean getShowRecipientEmail() {
    return showRecipientEmail;
  }

  public void setShowRecipientEmail(Boolean showRecipientEmail) {
    this.showRecipientEmail = showRecipientEmail;
  }


  public SignatureFrameSettings showTimeStamp(Boolean showTimeStamp) {
    this.showTimeStamp = showTimeStamp;
    return this;
  }

  /**
   * Get showTimeStamp
   * @return showTimeStamp
   */
  @javax.annotation.Nullable
  public Boolean getShowTimeStamp() {
    return showTimeStamp;
  }

  public void setShowTimeStamp(Boolean showTimeStamp) {
    this.showTimeStamp = showTimeStamp;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SignatureFrameSettings signatureFrameSettings = (SignatureFrameSettings) o;
    return Objects.equals(this.enableSignatureFrame, signatureFrameSettings.enableSignatureFrame) &&
        Objects.equals(this.showRecipientName, signatureFrameSettings.showRecipientName) &&
        Objects.equals(this.showRecipientEmail, signatureFrameSettings.showRecipientEmail) &&
        Objects.equals(this.showTimeStamp, signatureFrameSettings.showTimeStamp);
  }

  @Override
  public int hashCode() {
    return Objects.hash(enableSignatureFrame, showRecipientName, showRecipientEmail, showTimeStamp);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SignatureFrameSettings {\n");
    sb.append("    enableSignatureFrame: ").append(toIndentedString(enableSignatureFrame)).append("\n");
    sb.append("    showRecipientName: ").append(toIndentedString(showRecipientName)).append("\n");
    sb.append("    showRecipientEmail: ").append(toIndentedString(showRecipientEmail)).append("\n");
    sb.append("    showTimeStamp: ").append(toIndentedString(showTimeStamp)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (enableSignatureFrame != null) {
        if (isFileTypeOrListOfFiles(enableSignatureFrame)) {
            fileTypeFound = true;
        }

        if (enableSignatureFrame.getClass().equals(java.io.File.class) ||
            enableSignatureFrame.getClass().equals(Integer.class) ||
            enableSignatureFrame.getClass().equals(String.class) ||
            enableSignatureFrame.getClass().equals(java.net.URI.class)||
            enableSignatureFrame.getClass().isEnum()) {
            map.put("enableSignatureFrame", enableSignatureFrame);
        } else if (isListOfFile(enableSignatureFrame)) {
            for(int i = 0; i< getListSize(enableSignatureFrame); i++) {
                map.put("enableSignatureFrame", enableSignatureFrame);
            }
        }
        else {
          map.put("enableSignatureFrame", JSON.serialize(enableSignatureFrame));
        }
    }
    if (showRecipientName != null) {
        if (isFileTypeOrListOfFiles(showRecipientName)) {
            fileTypeFound = true;
        }

        if (showRecipientName.getClass().equals(java.io.File.class) ||
            showRecipientName.getClass().equals(Integer.class) ||
            showRecipientName.getClass().equals(String.class) ||
            showRecipientName.getClass().equals(java.net.URI.class)||
            showRecipientName.getClass().isEnum()) {
            map.put("showRecipientName", showRecipientName);
        } else if (isListOfFile(showRecipientName)) {
            for(int i = 0; i< getListSize(showRecipientName); i++) {
                map.put("showRecipientName", showRecipientName);
            }
        }
        else {
          map.put("showRecipientName", JSON.serialize(showRecipientName));
        }
    }
    if (showRecipientEmail != null) {
        if (isFileTypeOrListOfFiles(showRecipientEmail)) {
            fileTypeFound = true;
        }

        if (showRecipientEmail.getClass().equals(java.io.File.class) ||
            showRecipientEmail.getClass().equals(Integer.class) ||
            showRecipientEmail.getClass().equals(String.class) ||
            showRecipientEmail.getClass().equals(java.net.URI.class)||
            showRecipientEmail.getClass().isEnum()) {
            map.put("showRecipientEmail", showRecipientEmail);
        } else if (isListOfFile(showRecipientEmail)) {
            for(int i = 0; i< getListSize(showRecipientEmail); i++) {
                map.put("showRecipientEmail", showRecipientEmail);
            }
        }
        else {
          map.put("showRecipientEmail", JSON.serialize(showRecipientEmail));
        }
    }
    if (showTimeStamp != null) {
        if (isFileTypeOrListOfFiles(showTimeStamp)) {
            fileTypeFound = true;
        }

        if (showTimeStamp.getClass().equals(java.io.File.class) ||
            showTimeStamp.getClass().equals(Integer.class) ||
            showTimeStamp.getClass().equals(String.class) ||
            showTimeStamp.getClass().equals(java.net.URI.class)||
            showTimeStamp.getClass().isEnum()) {
            map.put("showTimeStamp", showTimeStamp);
        } else if (isListOfFile(showTimeStamp)) {
            for(int i = 0; i< getListSize(showTimeStamp); i++) {
                map.put("showTimeStamp", showTimeStamp);
            }
        }
        else {
          map.put("showTimeStamp", JSON.serialize(showTimeStamp));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("enableSignatureFrame");
    openapiFields.add("showRecipientName");
    openapiFields.add("showRecipientEmail");
    openapiFields.add("showTimeStamp");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SignatureFrameSettings
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SignatureFrameSettings.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SignatureFrameSettings is not found in the empty JSON string", SignatureFrameSettings.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SignatureFrameSettings.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SignatureFrameSettings' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SignatureFrameSettings> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SignatureFrameSettings.class));

       return (TypeAdapter<T>) new TypeAdapter<SignatureFrameSettings>() {
           @Override
           public void write(JsonWriter out, SignatureFrameSettings value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SignatureFrameSettings read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SignatureFrameSettings given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SignatureFrameSettings
   * @throws IOException if the JSON string is invalid with respect to SignatureFrameSettings
   */
  public static SignatureFrameSettings fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SignatureFrameSettings.class);
  }

  /**
   * Convert an instance of SignatureFrameSettings to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

