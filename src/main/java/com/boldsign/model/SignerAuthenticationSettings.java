/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * SignerAuthenticationSettings
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class SignerAuthenticationSettings {
  public static final String SERIALIZED_NAME_AUTHENTICATION_FREQUENCY = "authenticationFrequency";
  @SerializedName(SERIALIZED_NAME_AUTHENTICATION_FREQUENCY)
  private String authenticationFrequency;

  public static final String SERIALIZED_NAME_STATUS = "status";
  @SerializedName(SERIALIZED_NAME_STATUS)
  private String status;

  public SignerAuthenticationSettings() {
  }

  public SignerAuthenticationSettings authenticationFrequency(String authenticationFrequency) {
    this.authenticationFrequency = authenticationFrequency;
    return this;
  }

  /**
   * Get authenticationFrequency
   * @return authenticationFrequency
   */
  @javax.annotation.Nullable
  public String getAuthenticationFrequency() {
    return authenticationFrequency;
  }

  public void setAuthenticationFrequency(String authenticationFrequency) {
    this.authenticationFrequency = authenticationFrequency;
  }


  public SignerAuthenticationSettings status(String status) {
    this.status = status;
    return this;
  }

  /**
   * Get status
   * @return status
   */
  @javax.annotation.Nullable
  public String getStatus() {
    return status;
  }

  public void setStatus(String status) {
    this.status = status;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SignerAuthenticationSettings signerAuthenticationSettings = (SignerAuthenticationSettings) o;
    return Objects.equals(this.authenticationFrequency, signerAuthenticationSettings.authenticationFrequency) &&
        Objects.equals(this.status, signerAuthenticationSettings.status);
  }

  @Override
  public int hashCode() {
    return Objects.hash(authenticationFrequency, status);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SignerAuthenticationSettings {\n");
    sb.append("    authenticationFrequency: ").append(toIndentedString(authenticationFrequency)).append("\n");
    sb.append("    status: ").append(toIndentedString(status)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (authenticationFrequency != null) {
        if (isFileTypeOrListOfFiles(authenticationFrequency)) {
            fileTypeFound = true;
        }

        if (authenticationFrequency.getClass().equals(java.io.File.class) ||
            authenticationFrequency.getClass().equals(Integer.class) ||
            authenticationFrequency.getClass().equals(String.class) ||
            authenticationFrequency.getClass().equals(java.net.URI.class)||
            authenticationFrequency.getClass().isEnum()) {
            map.put("authenticationFrequency", authenticationFrequency);
        } else if (isListOfFile(authenticationFrequency)) {
            for(int i = 0; i< getListSize(authenticationFrequency); i++) {
                map.put("authenticationFrequency", authenticationFrequency);
            }
        }
        else {
          map.put("authenticationFrequency", JSON.serialize(authenticationFrequency));
        }
    }
    if (status != null) {
        if (isFileTypeOrListOfFiles(status)) {
            fileTypeFound = true;
        }

        if (status.getClass().equals(java.io.File.class) ||
            status.getClass().equals(Integer.class) ||
            status.getClass().equals(String.class) ||
            status.getClass().equals(java.net.URI.class)||
            status.getClass().isEnum()) {
            map.put("status", status);
        } else if (isListOfFile(status)) {
            for(int i = 0; i< getListSize(status); i++) {
                map.put("status", status);
            }
        }
        else {
          map.put("status", JSON.serialize(status));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("authenticationFrequency");
    openapiFields.add("status");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SignerAuthenticationSettings
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SignerAuthenticationSettings.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SignerAuthenticationSettings is not found in the empty JSON string", SignerAuthenticationSettings.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("authenticationFrequency") != null && !jsonObj.get("authenticationFrequency").isJsonNull()) && !jsonObj.get("authenticationFrequency").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `authenticationFrequency` to be a primitive type in the JSON string but got `%s`", jsonObj.get("authenticationFrequency").toString()));
      }
      if ((jsonObj.get("status") != null && !jsonObj.get("status").isJsonNull()) && !jsonObj.get("status").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `status` to be a primitive type in the JSON string but got `%s`", jsonObj.get("status").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SignerAuthenticationSettings.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SignerAuthenticationSettings' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SignerAuthenticationSettings> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SignerAuthenticationSettings.class));

       return (TypeAdapter<T>) new TypeAdapter<SignerAuthenticationSettings>() {
           @Override
           public void write(JsonWriter out, SignerAuthenticationSettings value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SignerAuthenticationSettings read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SignerAuthenticationSettings given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SignerAuthenticationSettings
   * @throws IOException if the JSON string is invalid with respect to SignerAuthenticationSettings
   */
  public static SignerAuthenticationSettings fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SignerAuthenticationSettings.class);
  }

  /**
   * Convert an instance of SignerAuthenticationSettings to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

