/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * CollaborationSettings
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class CollaborationSettings {
  public static final String SERIALIZED_NAME_IS_REQUIRED = "isRequired";
  @SerializedName(SERIALIZED_NAME_IS_REQUIRED)
  private Boolean isRequired;

  public static final String SERIALIZED_NAME_REQUIRE_SIGNER_APPROVAL = "requireSignerApproval";
  @SerializedName(SERIALIZED_NAME_REQUIRE_SIGNER_APPROVAL)
  private Boolean requireSignerApproval;

  public static final String SERIALIZED_NAME_REQUIRE_INITIAL = "requireInitial";
  @SerializedName(SERIALIZED_NAME_REQUIRE_INITIAL)
  private Boolean requireInitial;

  public static final String SERIALIZED_NAME_ALLOWED_SIGNERS = "allowedSigners";
  @SerializedName(SERIALIZED_NAME_ALLOWED_SIGNERS)
  private List<String> allowedSigners;

  public CollaborationSettings() {
  }

  public CollaborationSettings isRequired(Boolean isRequired) {
    this.isRequired = isRequired;
    return this;
  }

  /**
   * Get isRequired
   * @return isRequired
   */
  @javax.annotation.Nullable
  public Boolean getIsRequired() {
    return isRequired;
  }

  public void setIsRequired(Boolean isRequired) {
    this.isRequired = isRequired;
  }


  public CollaborationSettings requireSignerApproval(Boolean requireSignerApproval) {
    this.requireSignerApproval = requireSignerApproval;
    return this;
  }

  /**
   * Get requireSignerApproval
   * @return requireSignerApproval
   */
  @javax.annotation.Nullable
  public Boolean getRequireSignerApproval() {
    return requireSignerApproval;
  }

  public void setRequireSignerApproval(Boolean requireSignerApproval) {
    this.requireSignerApproval = requireSignerApproval;
  }


  public CollaborationSettings requireInitial(Boolean requireInitial) {
    this.requireInitial = requireInitial;
    return this;
  }

  /**
   * Get requireInitial
   * @return requireInitial
   */
  @javax.annotation.Nullable
  public Boolean getRequireInitial() {
    return requireInitial;
  }

  public void setRequireInitial(Boolean requireInitial) {
    this.requireInitial = requireInitial;
  }


  public CollaborationSettings allowedSigners(List<String> allowedSigners) {
    this.allowedSigners = allowedSigners;
    return this;
  }

  public CollaborationSettings addAllowedSignersItem(String allowedSignersItem) {
    if (this.allowedSigners == null) {
      this.allowedSigners = new ArrayList<>();
    }
    this.allowedSigners.add(allowedSignersItem);
    return this;
  }

  /**
   * Get allowedSigners
   * @return allowedSigners
   */
  @javax.annotation.Nullable
  public List<String> getAllowedSigners() {
    return allowedSigners;
  }

  public void setAllowedSigners(List<String> allowedSigners) {
    this.allowedSigners = allowedSigners;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    CollaborationSettings collaborationSettings = (CollaborationSettings) o;
    return Objects.equals(this.isRequired, collaborationSettings.isRequired) &&
        Objects.equals(this.requireSignerApproval, collaborationSettings.requireSignerApproval) &&
        Objects.equals(this.requireInitial, collaborationSettings.requireInitial) &&
        Objects.equals(this.allowedSigners, collaborationSettings.allowedSigners);
  }

  @Override
  public int hashCode() {
    return Objects.hash(isRequired, requireSignerApproval, requireInitial, allowedSigners);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class CollaborationSettings {\n");
    sb.append("    isRequired: ").append(toIndentedString(isRequired)).append("\n");
    sb.append("    requireSignerApproval: ").append(toIndentedString(requireSignerApproval)).append("\n");
    sb.append("    requireInitial: ").append(toIndentedString(requireInitial)).append("\n");
    sb.append("    allowedSigners: ").append(toIndentedString(allowedSigners)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (isRequired != null) {
        if (isFileTypeOrListOfFiles(isRequired)) {
            fileTypeFound = true;
        }

        if (isRequired.getClass().equals(java.io.File.class) ||
            isRequired.getClass().equals(Integer.class) ||
            isRequired.getClass().equals(String.class) ||
            isRequired.getClass().equals(java.net.URI.class)||
            isRequired.getClass().isEnum()) {
            map.put("isRequired", isRequired);
        } else if (isListOfFile(isRequired)) {
            for(int i = 0; i< getListSize(isRequired); i++) {
                map.put("isRequired", isRequired);
            }
        }
        else {
          map.put("isRequired", JSON.serialize(isRequired));
        }
    }
    if (requireSignerApproval != null) {
        if (isFileTypeOrListOfFiles(requireSignerApproval)) {
            fileTypeFound = true;
        }

        if (requireSignerApproval.getClass().equals(java.io.File.class) ||
            requireSignerApproval.getClass().equals(Integer.class) ||
            requireSignerApproval.getClass().equals(String.class) ||
            requireSignerApproval.getClass().equals(java.net.URI.class)||
            requireSignerApproval.getClass().isEnum()) {
            map.put("requireSignerApproval", requireSignerApproval);
        } else if (isListOfFile(requireSignerApproval)) {
            for(int i = 0; i< getListSize(requireSignerApproval); i++) {
                map.put("requireSignerApproval", requireSignerApproval);
            }
        }
        else {
          map.put("requireSignerApproval", JSON.serialize(requireSignerApproval));
        }
    }
    if (requireInitial != null) {
        if (isFileTypeOrListOfFiles(requireInitial)) {
            fileTypeFound = true;
        }

        if (requireInitial.getClass().equals(java.io.File.class) ||
            requireInitial.getClass().equals(Integer.class) ||
            requireInitial.getClass().equals(String.class) ||
            requireInitial.getClass().equals(java.net.URI.class)||
            requireInitial.getClass().isEnum()) {
            map.put("requireInitial", requireInitial);
        } else if (isListOfFile(requireInitial)) {
            for(int i = 0; i< getListSize(requireInitial); i++) {
                map.put("requireInitial", requireInitial);
            }
        }
        else {
          map.put("requireInitial", JSON.serialize(requireInitial));
        }
    }
    if (allowedSigners != null) {
        if (isFileTypeOrListOfFiles(allowedSigners)) {
            fileTypeFound = true;
        }

        if (allowedSigners.getClass().equals(java.io.File.class) ||
            allowedSigners.getClass().equals(Integer.class) ||
            allowedSigners.getClass().equals(String.class) ||
            allowedSigners.getClass().equals(java.net.URI.class)||
            allowedSigners.getClass().isEnum()) {
            map.put("allowedSigners", allowedSigners);
        } else if (isListOfFile(allowedSigners)) {
            for(int i = 0; i< getListSize(allowedSigners); i++) {
                map.put("allowedSigners", allowedSigners);
            }
        }
        else {
          List<String> objectList = new ArrayList<String>();
          for(Object item : allowedSigners) {
            if(item instanceof URI || item instanceof String || item instanceof Integer || item instanceof Enum) {
              objectList.add(item.toString());
            }
            else if (item instanceof EditDocumentFile) {
              int index = 0;
              EditDocumentFile fileItem = (EditDocumentFile) item;
              for (String field : EditDocumentFile.openapiFields) {
                String methodName = "get" + field.substring(0, 1).toUpperCase() + field.substring(1);
                Method getter = EditDocumentFile.class.getMethod(methodName);
                Object value = getter.invoke(fileItem);
                String key = "files[" + index + "]." + field;
                if (value != null){
                  if(field == "file"){
                    fileTypeFound = true;
                  }
                map.put(key, value);
                }
              }
              index++;
            }
            else {
              String objectData = JSON.serialize(item);
              objectList.add(objectData);
            }
          }
          map.put("allowedSigners", objectList);
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("isRequired");
    openapiFields.add("requireSignerApproval");
    openapiFields.add("requireInitial");
    openapiFields.add("allowedSigners");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to CollaborationSettings
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!CollaborationSettings.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in CollaborationSettings is not found in the empty JSON string", CollaborationSettings.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("allowedSigners") != null && !jsonObj.get("allowedSigners").isJsonNull() && !jsonObj.get("allowedSigners").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `allowedSigners` to be an array in the JSON string but got `%s`", jsonObj.get("allowedSigners").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!CollaborationSettings.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'CollaborationSettings' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<CollaborationSettings> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(CollaborationSettings.class));

       return (TypeAdapter<T>) new TypeAdapter<CollaborationSettings>() {
           @Override
           public void write(JsonWriter out, CollaborationSettings value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public CollaborationSettings read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of CollaborationSettings given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of CollaborationSettings
   * @throws IOException if the JSON string is invalid with respect to CollaborationSettings
   */
  public static CollaborationSettings fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CollaborationSettings.class);
  }

  /**
   * Convert an instance of CollaborationSettings to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

