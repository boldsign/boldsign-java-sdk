/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * Added
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class Added {
  public static final String SERIALIZED_NAME_SIGNER_NAME = "signerName";
  @SerializedName(SERIALIZED_NAME_SIGNER_NAME)
  private String signerName;

  public static final String SERIALIZED_NAME_SIGNER_EMAIL = "signerEmail";
  @SerializedName(SERIALIZED_NAME_SIGNER_EMAIL)
  private String signerEmail;

  public static final String SERIALIZED_NAME_SIGNER_PHONE_NUMBER = "signerPhoneNumber";
  @SerializedName(SERIALIZED_NAME_SIGNER_PHONE_NUMBER)
  private String signerPhoneNumber;

  public Added() {
  }

  public Added signerName(String signerName) {
    this.signerName = signerName;
    return this;
  }

  /**
   * Get signerName
   * @return signerName
   */
  @javax.annotation.Nullable
  public String getSignerName() {
    return signerName;
  }

  public void setSignerName(String signerName) {
    this.signerName = signerName;
  }


  public Added signerEmail(String signerEmail) {
    this.signerEmail = signerEmail;
    return this;
  }

  /**
   * Get signerEmail
   * @return signerEmail
   */
  @javax.annotation.Nullable
  public String getSignerEmail() {
    return signerEmail;
  }

  public void setSignerEmail(String signerEmail) {
    this.signerEmail = signerEmail;
  }


  public Added signerPhoneNumber(String signerPhoneNumber) {
    this.signerPhoneNumber = signerPhoneNumber;
    return this;
  }

  /**
   * Get signerPhoneNumber
   * @return signerPhoneNumber
   */
  @javax.annotation.Nullable
  public String getSignerPhoneNumber() {
    return signerPhoneNumber;
  }

  public void setSignerPhoneNumber(String signerPhoneNumber) {
    this.signerPhoneNumber = signerPhoneNumber;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    Added added = (Added) o;
    return Objects.equals(this.signerName, added.signerName) &&
        Objects.equals(this.signerEmail, added.signerEmail) &&
        Objects.equals(this.signerPhoneNumber, added.signerPhoneNumber);
  }

  @Override
  public int hashCode() {
    return Objects.hash(signerName, signerEmail, signerPhoneNumber);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class Added {\n");
    sb.append("    signerName: ").append(toIndentedString(signerName)).append("\n");
    sb.append("    signerEmail: ").append(toIndentedString(signerEmail)).append("\n");
    sb.append("    signerPhoneNumber: ").append(toIndentedString(signerPhoneNumber)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (signerName != null) {
        if (isFileTypeOrListOfFiles(signerName)) {
            fileTypeFound = true;
        }

        if (signerName.getClass().equals(java.io.File.class) ||
            signerName.getClass().equals(Integer.class) ||
            signerName.getClass().equals(String.class) ||
            signerName.getClass().equals(java.net.URI.class)||
            signerName.getClass().isEnum()) {
            map.put("signerName", signerName);
        } else if (isListOfFile(signerName)) {
            for(int i = 0; i< getListSize(signerName); i++) {
                map.put("signerName", signerName);
            }
        }
        else {
          map.put("signerName", JSON.serialize(signerName));
        }
    }
    if (signerEmail != null) {
        if (isFileTypeOrListOfFiles(signerEmail)) {
            fileTypeFound = true;
        }

        if (signerEmail.getClass().equals(java.io.File.class) ||
            signerEmail.getClass().equals(Integer.class) ||
            signerEmail.getClass().equals(String.class) ||
            signerEmail.getClass().equals(java.net.URI.class)||
            signerEmail.getClass().isEnum()) {
            map.put("signerEmail", signerEmail);
        } else if (isListOfFile(signerEmail)) {
            for(int i = 0; i< getListSize(signerEmail); i++) {
                map.put("signerEmail", signerEmail);
            }
        }
        else {
          map.put("signerEmail", JSON.serialize(signerEmail));
        }
    }
    if (signerPhoneNumber != null) {
        if (isFileTypeOrListOfFiles(signerPhoneNumber)) {
            fileTypeFound = true;
        }

        if (signerPhoneNumber.getClass().equals(java.io.File.class) ||
            signerPhoneNumber.getClass().equals(Integer.class) ||
            signerPhoneNumber.getClass().equals(String.class) ||
            signerPhoneNumber.getClass().equals(java.net.URI.class)||
            signerPhoneNumber.getClass().isEnum()) {
            map.put("signerPhoneNumber", signerPhoneNumber);
        } else if (isListOfFile(signerPhoneNumber)) {
            for(int i = 0; i< getListSize(signerPhoneNumber); i++) {
                map.put("signerPhoneNumber", signerPhoneNumber);
            }
        }
        else {
          map.put("signerPhoneNumber", JSON.serialize(signerPhoneNumber));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("signerName");
    openapiFields.add("signerEmail");
    openapiFields.add("signerPhoneNumber");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to Added
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!Added.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in Added is not found in the empty JSON string", Added.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("signerName") != null && !jsonObj.get("signerName").isJsonNull()) && !jsonObj.get("signerName").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `signerName` to be a primitive type in the JSON string but got `%s`", jsonObj.get("signerName").toString()));
      }
      if ((jsonObj.get("signerEmail") != null && !jsonObj.get("signerEmail").isJsonNull()) && !jsonObj.get("signerEmail").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `signerEmail` to be a primitive type in the JSON string but got `%s`", jsonObj.get("signerEmail").toString()));
      }
      if ((jsonObj.get("signerPhoneNumber") != null && !jsonObj.get("signerPhoneNumber").isJsonNull()) && !jsonObj.get("signerPhoneNumber").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `signerPhoneNumber` to be a primitive type in the JSON string but got `%s`", jsonObj.get("signerPhoneNumber").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!Added.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'Added' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<Added> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(Added.class));

       return (TypeAdapter<T>) new TypeAdapter<Added>() {
           @Override
           public void write(JsonWriter out, Added value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public Added read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of Added given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of Added
   * @throws IOException if the JSON string is invalid with respect to Added
   */
  public static Added fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, Added.class);
  }

  /**
   * Convert an instance of Added to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

