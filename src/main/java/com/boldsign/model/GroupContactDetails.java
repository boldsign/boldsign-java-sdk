/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.boldsign.model.GroupUser;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * GroupContactDetails
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class GroupContactDetails {
  public static final String SERIALIZED_NAME_GROUP_NAME = "groupName";
  @SerializedName(SERIALIZED_NAME_GROUP_NAME)
  private String groupName;

  public static final String SERIALIZED_NAME_CONTACTS = "contacts";
  @SerializedName(SERIALIZED_NAME_CONTACTS)
  private List<GroupUser> contacts = new ArrayList<>();

  public static final String SERIALIZED_NAME_DIRECTORIES = "directories";
  @SerializedName(SERIALIZED_NAME_DIRECTORIES)
  private List<String> directories;

  public GroupContactDetails() {
  }

  public GroupContactDetails groupName(String groupName) {
    this.groupName = groupName;
    return this;
  }

  /**
   * Get groupName
   * @return groupName
   */
  @javax.annotation.Nonnull
  public String getGroupName() {
    return groupName;
  }

  public void setGroupName(String groupName) {
    this.groupName = groupName;
  }


  public GroupContactDetails contacts(List<GroupUser> contacts) {
    this.contacts = contacts;
    return this;
  }

  public GroupContactDetails addContactsItem(GroupUser contactsItem) {
    if (this.contacts == null) {
      this.contacts = new ArrayList<>();
    }
    this.contacts.add(contactsItem);
    return this;
  }

  /**
   * Get contacts
   * @return contacts
   */
  @javax.annotation.Nonnull
  public List<GroupUser> getContacts() {
    return contacts;
  }

  public void setContacts(List<GroupUser> contacts) {
    this.contacts = contacts;
  }


  public GroupContactDetails directories(List<String> directories) {
    this.directories = directories;
    return this;
  }

  public GroupContactDetails addDirectoriesItem(String directoriesItem) {
    if (this.directories == null) {
      this.directories = new ArrayList<>();
    }
    this.directories.add(directoriesItem);
    return this;
  }

  /**
   * Get directories
   * @return directories
   */
  @javax.annotation.Nullable
  public List<String> getDirectories() {
    return directories;
  }

  public void setDirectories(List<String> directories) {
    this.directories = directories;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    GroupContactDetails groupContactDetails = (GroupContactDetails) o;
    return Objects.equals(this.groupName, groupContactDetails.groupName) &&
        Objects.equals(this.contacts, groupContactDetails.contacts) &&
        Objects.equals(this.directories, groupContactDetails.directories);
  }

  @Override
  public int hashCode() {
    return Objects.hash(groupName, contacts, directories);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class GroupContactDetails {\n");
    sb.append("    groupName: ").append(toIndentedString(groupName)).append("\n");
    sb.append("    contacts: ").append(toIndentedString(contacts)).append("\n");
    sb.append("    directories: ").append(toIndentedString(directories)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (groupName != null) {
        if (isFileTypeOrListOfFiles(groupName)) {
            fileTypeFound = true;
        }

        if (groupName.getClass().equals(java.io.File.class) ||
            groupName.getClass().equals(Integer.class) ||
            groupName.getClass().equals(String.class) ||
            groupName.getClass().equals(java.net.URI.class)||
            groupName.getClass().isEnum()) {
            map.put("groupName", groupName);
        } else if (isListOfFile(groupName)) {
            for(int i = 0; i< getListSize(groupName); i++) {
                map.put("groupName", groupName);
            }
        }
        else {
          map.put("groupName", JSON.serialize(groupName));
        }
    }
    if (contacts != null) {
        if (isFileTypeOrListOfFiles(contacts)) {
            fileTypeFound = true;
        }

        if (contacts.getClass().equals(java.io.File.class) ||
            contacts.getClass().equals(Integer.class) ||
            contacts.getClass().equals(String.class) ||
            contacts.getClass().equals(java.net.URI.class)||
            contacts.getClass().isEnum()) {
            map.put("contacts", contacts);
        } else if (isListOfFile(contacts)) {
            for(int i = 0; i< getListSize(contacts); i++) {
                map.put("contacts", contacts);
            }
        }
        else {
          List<String> objectList = new ArrayList<String>();
          for(Object item : contacts) {
            if(item instanceof URI || item instanceof String || item instanceof Integer || item instanceof Enum) {
              objectList.add(item.toString());
            }
            else if (item instanceof EditDocumentFile) {
              int index = 0;
              EditDocumentFile fileItem = (EditDocumentFile) item;
              for (String field : EditDocumentFile.openapiFields) {
                String methodName = "get" + field.substring(0, 1).toUpperCase() + field.substring(1);
                Method getter = EditDocumentFile.class.getMethod(methodName);
                Object value = getter.invoke(fileItem);
                String key = "files[" + index + "]." + field;
                if (value != null){
                  if(field == "file"){
                    fileTypeFound = true;
                  }
                map.put(key, value);
                }
              }
              index++;
            }
            else {
              String objectData = JSON.serialize(item);
              objectList.add(objectData);
            }
          }
          map.put("contacts", objectList);
        }
    }
    if (directories != null) {
        if (isFileTypeOrListOfFiles(directories)) {
            fileTypeFound = true;
        }

        if (directories.getClass().equals(java.io.File.class) ||
            directories.getClass().equals(Integer.class) ||
            directories.getClass().equals(String.class) ||
            directories.getClass().equals(java.net.URI.class)||
            directories.getClass().isEnum()) {
            map.put("directories", directories);
        } else if (isListOfFile(directories)) {
            for(int i = 0; i< getListSize(directories); i++) {
                map.put("directories", directories);
            }
        }
        else {
          List<String> objectList = new ArrayList<String>();
          for(Object item : directories) {
            if(item instanceof URI || item instanceof String || item instanceof Integer || item instanceof Enum) {
              objectList.add(item.toString());
            }
            else if (item instanceof EditDocumentFile) {
              int index = 0;
              EditDocumentFile fileItem = (EditDocumentFile) item;
              for (String field : EditDocumentFile.openapiFields) {
                String methodName = "get" + field.substring(0, 1).toUpperCase() + field.substring(1);
                Method getter = EditDocumentFile.class.getMethod(methodName);
                Object value = getter.invoke(fileItem);
                String key = "files[" + index + "]." + field;
                if (value != null){
                  if(field == "file"){
                    fileTypeFound = true;
                  }
                map.put(key, value);
                }
              }
              index++;
            }
            else {
              String objectData = JSON.serialize(item);
              objectList.add(objectData);
            }
          }
          map.put("directories", objectList);
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("groupName");
    openapiFields.add("contacts");
    openapiFields.add("directories");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("groupName");
    openapiRequiredFields.add("contacts");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to GroupContactDetails
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!GroupContactDetails.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in GroupContactDetails is not found in the empty JSON string", GroupContactDetails.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : GroupContactDetails.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("groupName").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `groupName` to be a primitive type in the JSON string but got `%s`", jsonObj.get("groupName").toString()));
      }
      // ensure the json data is an array
      if (!jsonObj.get("contacts").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `contacts` to be an array in the JSON string but got `%s`", jsonObj.get("contacts").toString()));
      }

      JsonArray jsonArraycontacts = jsonObj.getAsJsonArray("contacts");
      // validate the required field `contacts` (array)
      for (int i = 0; i < jsonArraycontacts.size(); i++) {
        GroupUser.validateJsonElement(jsonArraycontacts.get(i));
      };
      // ensure the optional json data is an array if present
      if (jsonObj.get("directories") != null && !jsonObj.get("directories").isJsonNull() && !jsonObj.get("directories").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `directories` to be an array in the JSON string but got `%s`", jsonObj.get("directories").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!GroupContactDetails.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'GroupContactDetails' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<GroupContactDetails> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(GroupContactDetails.class));

       return (TypeAdapter<T>) new TypeAdapter<GroupContactDetails>() {
           @Override
           public void write(JsonWriter out, GroupContactDetails value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public GroupContactDetails read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of GroupContactDetails given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of GroupContactDetails
   * @throws IOException if the JSON string is invalid with respect to GroupContactDetails
   */
  public static GroupContactDetails fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, GroupContactDetails.class);
  }

  /**
   * Convert an instance of GroupContactDetails to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

