/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.File;
import java.io.IOException;
import java.net.URI;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * EditDocumentFile
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class EditDocumentFile {
  /**
   * Gets or Sets editAction
   */
  @JsonAdapter(EditActionEnum.Adapter.class)
  public enum EditActionEnum {
    ADD("Add"),
    
    UPDATE("Update"),
    
    REMOVE("Remove");

    private String value;

    EditActionEnum(String value) {
      this.value = value;
    }

    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    public static EditActionEnum fromValue(String value) {
      for (EditActionEnum b : EditActionEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }

    public static class Adapter extends TypeAdapter<EditActionEnum> {
      @Override
      public void write(final JsonWriter jsonWriter, final EditActionEnum enumeration) throws IOException {
        jsonWriter.value(enumeration.getValue());
      }

      @Override
      public EditActionEnum read(final JsonReader jsonReader) throws IOException {
        String value =  jsonReader.nextString();
        return EditActionEnum.fromValue(value);
      }
    }

    public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      String value = jsonElement.getAsString();
      EditActionEnum.fromValue(value);
    }
  }

  public static final String SERIALIZED_NAME_EDIT_ACTION = "editAction";
  @SerializedName(SERIALIZED_NAME_EDIT_ACTION)
  private EditActionEnum editAction;

  public static final String SERIALIZED_NAME_FILE = "file";
  @SerializedName(SERIALIZED_NAME_FILE)
  private File _file;

  public static final String SERIALIZED_NAME_FILE_URL = "fileUrl";
  @SerializedName(SERIALIZED_NAME_FILE_URL)
  private URI fileUrl;

  public static final String SERIALIZED_NAME_ID = "id";
  @SerializedName(SERIALIZED_NAME_ID)
  private String id;

  public EditDocumentFile() {
  }

  public EditDocumentFile editAction(EditActionEnum editAction) {
    this.editAction = editAction;
    return this;
  }

  /**
   * Get editAction
   * @return editAction
   */
  @javax.annotation.Nonnull
  public EditActionEnum getEditAction() {
    return editAction;
  }

  public void setEditAction(EditActionEnum editAction) {
    this.editAction = editAction;
  }


  public EditDocumentFile _file(File _file) {
    this._file = _file;
    return this;
  }

  /**
   * Get _file
   * @return _file
   */
  @javax.annotation.Nullable
  public File getFile() {
    return _file;
  }

  public void setFile(File _file) {
    this._file = _file;
  }


  public EditDocumentFile fileUrl(URI fileUrl) {
    this.fileUrl = fileUrl;
    return this;
  }

  /**
   * Get fileUrl
   * @return fileUrl
   */
  @javax.annotation.Nullable
  public URI getFileUrl() {
    return fileUrl;
  }

  public void setFileUrl(URI fileUrl) {
    this.fileUrl = fileUrl;
  }


  public EditDocumentFile id(String id) {
    this.id = id;
    return this;
  }

  /**
   * Get id
   * @return id
   */
  @javax.annotation.Nullable
  public String getId() {
    return id;
  }

  public void setId(String id) {
    this.id = id;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    EditDocumentFile editDocumentFile = (EditDocumentFile) o;
    return Objects.equals(this.editAction, editDocumentFile.editAction) &&
        Objects.equals(this._file, editDocumentFile._file) &&
        Objects.equals(this.fileUrl, editDocumentFile.fileUrl) &&
        Objects.equals(this.id, editDocumentFile.id);
  }

  @Override
  public int hashCode() {
    return Objects.hash(editAction, _file, fileUrl, id);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class EditDocumentFile {\n");
    sb.append("    editAction: ").append(toIndentedString(editAction)).append("\n");
    sb.append("    _file: ").append(toIndentedString(_file)).append("\n");
    sb.append("    fileUrl: ").append(toIndentedString(fileUrl)).append("\n");
    sb.append("    id: ").append(toIndentedString(id)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (editAction != null) {
        if (isFileTypeOrListOfFiles(editAction)) {
            fileTypeFound = true;
        }

        if (editAction.getClass().equals(java.io.File.class) ||
            editAction.getClass().equals(Integer.class) ||
            editAction.getClass().equals(String.class) ||
            editAction.getClass().equals(java.net.URI.class)||
            editAction.getClass().isEnum()) {
            map.put("editAction", editAction);
        } else if (isListOfFile(editAction)) {
            for(int i = 0; i< getListSize(editAction); i++) {
                map.put("editAction", editAction);
            }
        }
        else {
          map.put("editAction", JSON.serialize(editAction));
        }
    }
    if (_file != null) {
        if (isFileTypeOrListOfFiles(_file)) {
            fileTypeFound = true;
        }

        if (_file.getClass().equals(java.io.File.class) ||
            _file.getClass().equals(Integer.class) ||
            _file.getClass().equals(String.class) ||
            _file.getClass().equals(java.net.URI.class)||
            _file.getClass().isEnum()) {
            map.put("file", _file);
        } else if (isListOfFile(_file)) {
            for(int i = 0; i< getListSize(_file); i++) {
                map.put("file", _file);
            }
        }
        else {
          map.put("file", JSON.serialize(_file));
        }
    }
    if (fileUrl != null) {
        if (isFileTypeOrListOfFiles(fileUrl)) {
            fileTypeFound = true;
        }

        if (fileUrl.getClass().equals(java.io.File.class) ||
            fileUrl.getClass().equals(Integer.class) ||
            fileUrl.getClass().equals(String.class) ||
            fileUrl.getClass().equals(java.net.URI.class)||
            fileUrl.getClass().isEnum()) {
            map.put("fileUrl", fileUrl);
        } else if (isListOfFile(fileUrl)) {
            for(int i = 0; i< getListSize(fileUrl); i++) {
                map.put("fileUrl", fileUrl);
            }
        }
        else {
          map.put("fileUrl", JSON.serialize(fileUrl));
        }
    }
    if (id != null) {
        if (isFileTypeOrListOfFiles(id)) {
            fileTypeFound = true;
        }

        if (id.getClass().equals(java.io.File.class) ||
            id.getClass().equals(Integer.class) ||
            id.getClass().equals(String.class) ||
            id.getClass().equals(java.net.URI.class)||
            id.getClass().isEnum()) {
            map.put("id", id);
        } else if (isListOfFile(id)) {
            for(int i = 0; i< getListSize(id); i++) {
                map.put("id", id);
            }
        }
        else {
          map.put("id", JSON.serialize(id));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("editAction");
    openapiFields.add("file");
    openapiFields.add("fileUrl");
    openapiFields.add("id");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
    openapiRequiredFields.add("editAction");
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to EditDocumentFile
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!EditDocumentFile.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in EditDocumentFile is not found in the empty JSON string", EditDocumentFile.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 

      // check to make sure all required properties/fields are present in the JSON string
      for (String requiredField : EditDocumentFile.openapiRequiredFields) {
        if (jsonElement.getAsJsonObject().get(requiredField) == null) {
          throw new IllegalArgumentException(String.format("The required field `%s` is not found in the JSON string: %s", requiredField, jsonElement.toString()));
        }
      }
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if (!jsonObj.get("editAction").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `editAction` to be a primitive type in the JSON string but got `%s`", jsonObj.get("editAction").toString()));
      }
      // validate the required field `editAction`
      EditActionEnum.validateJsonElement(jsonObj.get("editAction"));
      if ((jsonObj.get("fileUrl") != null && !jsonObj.get("fileUrl").isJsonNull()) && !jsonObj.get("fileUrl").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `fileUrl` to be a primitive type in the JSON string but got `%s`", jsonObj.get("fileUrl").toString()));
      }
      if ((jsonObj.get("id") != null && !jsonObj.get("id").isJsonNull()) && !jsonObj.get("id").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `id` to be a primitive type in the JSON string but got `%s`", jsonObj.get("id").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!EditDocumentFile.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'EditDocumentFile' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<EditDocumentFile> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(EditDocumentFile.class));

       return (TypeAdapter<T>) new TypeAdapter<EditDocumentFile>() {
           @Override
           public void write(JsonWriter out, EditDocumentFile value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public EditDocumentFile read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of EditDocumentFile given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of EditDocumentFile
   * @throws IOException if the JSON string is invalid with respect to EditDocumentFile
   */
  public static EditDocumentFile fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, EditDocumentFile.class);
  }

  /**
   * Convert an instance of EditDocumentFile to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

