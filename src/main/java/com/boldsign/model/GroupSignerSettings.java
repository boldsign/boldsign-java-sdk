/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * GroupSignerSettings
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class GroupSignerSettings {
  public static final String SERIALIZED_NAME_ENABLED = "enabled";
  @SerializedName(SERIALIZED_NAME_ENABLED)
  private Boolean enabled;

  public static final String SERIALIZED_NAME_ALLOWED_DIRECTORIES = "allowedDirectories";
  @SerializedName(SERIALIZED_NAME_ALLOWED_DIRECTORIES)
  private List<String> allowedDirectories;

  public GroupSignerSettings() {
  }

  public GroupSignerSettings enabled(Boolean enabled) {
    this.enabled = enabled;
    return this;
  }

  /**
   * Get enabled
   * @return enabled
   */
  @javax.annotation.Nullable
  public Boolean getEnabled() {
    return enabled;
  }

  public void setEnabled(Boolean enabled) {
    this.enabled = enabled;
  }


  public GroupSignerSettings allowedDirectories(List<String> allowedDirectories) {
    this.allowedDirectories = allowedDirectories;
    return this;
  }

  public GroupSignerSettings addAllowedDirectoriesItem(String allowedDirectoriesItem) {
    if (this.allowedDirectories == null) {
      this.allowedDirectories = new ArrayList<>();
    }
    this.allowedDirectories.add(allowedDirectoriesItem);
    return this;
  }

  /**
   * Get allowedDirectories
   * @return allowedDirectories
   */
  @javax.annotation.Nullable
  public List<String> getAllowedDirectories() {
    return allowedDirectories;
  }

  public void setAllowedDirectories(List<String> allowedDirectories) {
    this.allowedDirectories = allowedDirectories;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    GroupSignerSettings groupSignerSettings = (GroupSignerSettings) o;
    return Objects.equals(this.enabled, groupSignerSettings.enabled) &&
        Objects.equals(this.allowedDirectories, groupSignerSettings.allowedDirectories);
  }

  @Override
  public int hashCode() {
    return Objects.hash(enabled, allowedDirectories);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class GroupSignerSettings {\n");
    sb.append("    enabled: ").append(toIndentedString(enabled)).append("\n");
    sb.append("    allowedDirectories: ").append(toIndentedString(allowedDirectories)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (enabled != null) {
        if (isFileTypeOrListOfFiles(enabled)) {
            fileTypeFound = true;
        }

        if (enabled.getClass().equals(java.io.File.class) ||
            enabled.getClass().equals(Integer.class) ||
            enabled.getClass().equals(String.class) ||
            enabled.getClass().equals(java.net.URI.class)||
            enabled.getClass().isEnum()) {
            map.put("enabled", enabled);
        } else if (isListOfFile(enabled)) {
            for(int i = 0; i< getListSize(enabled); i++) {
                map.put("enabled", enabled);
            }
        }
        else {
          map.put("enabled", JSON.serialize(enabled));
        }
    }
    if (allowedDirectories != null) {
        if (isFileTypeOrListOfFiles(allowedDirectories)) {
            fileTypeFound = true;
        }

        if (allowedDirectories.getClass().equals(java.io.File.class) ||
            allowedDirectories.getClass().equals(Integer.class) ||
            allowedDirectories.getClass().equals(String.class) ||
            allowedDirectories.getClass().equals(java.net.URI.class)||
            allowedDirectories.getClass().isEnum()) {
            map.put("allowedDirectories", allowedDirectories);
        } else if (isListOfFile(allowedDirectories)) {
            for(int i = 0; i< getListSize(allowedDirectories); i++) {
                map.put("allowedDirectories", allowedDirectories);
            }
        }
        else {
          List<String> objectList = new ArrayList<String>();
          for(Object item : allowedDirectories) {
            if(item instanceof URI || item instanceof String || item instanceof Integer || item instanceof Enum) {
              objectList.add(item.toString());
            }
            else if (item instanceof EditDocumentFile) {
              int index = 0;
              EditDocumentFile fileItem = (EditDocumentFile) item;
              for (String field : EditDocumentFile.openapiFields) {
                String methodName = "get" + field.substring(0, 1).toUpperCase() + field.substring(1);
                Method getter = EditDocumentFile.class.getMethod(methodName);
                Object value = getter.invoke(fileItem);
                String key = "files[" + index + "]." + field;
                if (value != null){
                  if(field == "file"){
                    fileTypeFound = true;
                  }
                map.put(key, value);
                }
              }
              index++;
            }
            else {
              String objectData = JSON.serialize(item);
              objectList.add(objectData);
            }
          }
          map.put("allowedDirectories", objectList);
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("enabled");
    openapiFields.add("allowedDirectories");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to GroupSignerSettings
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!GroupSignerSettings.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in GroupSignerSettings is not found in the empty JSON string", GroupSignerSettings.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // ensure the optional json data is an array if present
      if (jsonObj.get("allowedDirectories") != null && !jsonObj.get("allowedDirectories").isJsonNull() && !jsonObj.get("allowedDirectories").isJsonArray()) {
        throw new IllegalArgumentException(String.format("Expected the field `allowedDirectories` to be an array in the JSON string but got `%s`", jsonObj.get("allowedDirectories").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!GroupSignerSettings.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'GroupSignerSettings' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<GroupSignerSettings> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(GroupSignerSettings.class));

       return (TypeAdapter<T>) new TypeAdapter<GroupSignerSettings>() {
           @Override
           public void write(JsonWriter out, GroupSignerSettings value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public GroupSignerSettings read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of GroupSignerSettings given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of GroupSignerSettings
   * @throws IOException if the JSON string is invalid with respect to GroupSignerSettings
   */
  public static GroupSignerSettings fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, GroupSignerSettings.class);
  }

  /**
   * Convert an instance of GroupSignerSettings to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

