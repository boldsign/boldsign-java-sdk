/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * SenderIdentityCreated
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class SenderIdentityCreated {
  public static final String SERIALIZED_NAME_SENDER_IDENTITY_ID = "senderIdentityId";
  @SerializedName(SERIALIZED_NAME_SENDER_IDENTITY_ID)
  private String senderIdentityId;

  public SenderIdentityCreated() {
  }

  public SenderIdentityCreated senderIdentityId(String senderIdentityId) {
    this.senderIdentityId = senderIdentityId;
    return this;
  }

  /**
   * Get senderIdentityId
   * @return senderIdentityId
   */
  @javax.annotation.Nullable
  public String getSenderIdentityId() {
    return senderIdentityId;
  }

  public void setSenderIdentityId(String senderIdentityId) {
    this.senderIdentityId = senderIdentityId;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    SenderIdentityCreated senderIdentityCreated = (SenderIdentityCreated) o;
    return Objects.equals(this.senderIdentityId, senderIdentityCreated.senderIdentityId);
  }

  @Override
  public int hashCode() {
    return Objects.hash(senderIdentityId);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class SenderIdentityCreated {\n");
    sb.append("    senderIdentityId: ").append(toIndentedString(senderIdentityId)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (senderIdentityId != null) {
        if (isFileTypeOrListOfFiles(senderIdentityId)) {
            fileTypeFound = true;
        }

        if (senderIdentityId.getClass().equals(java.io.File.class) ||
            senderIdentityId.getClass().equals(Integer.class) ||
            senderIdentityId.getClass().equals(String.class) ||
            senderIdentityId.getClass().equals(java.net.URI.class)||
            senderIdentityId.getClass().isEnum()) {
            map.put("senderIdentityId", senderIdentityId);
        } else if (isListOfFile(senderIdentityId)) {
            for(int i = 0; i< getListSize(senderIdentityId); i++) {
                map.put("senderIdentityId", senderIdentityId);
            }
        }
        else {
          map.put("senderIdentityId", JSON.serialize(senderIdentityId));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("senderIdentityId");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to SenderIdentityCreated
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!SenderIdentityCreated.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in SenderIdentityCreated is not found in the empty JSON string", SenderIdentityCreated.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("senderIdentityId") != null && !jsonObj.get("senderIdentityId").isJsonNull()) && !jsonObj.get("senderIdentityId").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `senderIdentityId` to be a primitive type in the JSON string but got `%s`", jsonObj.get("senderIdentityId").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!SenderIdentityCreated.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'SenderIdentityCreated' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<SenderIdentityCreated> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(SenderIdentityCreated.class));

       return (TypeAdapter<T>) new TypeAdapter<SenderIdentityCreated>() {
           @Override
           public void write(JsonWriter out, SenderIdentityCreated value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public SenderIdentityCreated read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of SenderIdentityCreated given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of SenderIdentityCreated
   * @throws IOException if the JSON string is invalid with respect to SenderIdentityCreated
   */
  public static SenderIdentityCreated fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, SenderIdentityCreated.class);
  }

  /**
   * Convert an instance of SenderIdentityCreated to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

