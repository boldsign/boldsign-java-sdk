/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.net.URI;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * EmbeddedDocumentEdited
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class EmbeddedDocumentEdited {
  public static final String SERIALIZED_NAME_EDIT_URL = "editUrl";
  @SerializedName(SERIALIZED_NAME_EDIT_URL)
  private URI editUrl;

  public EmbeddedDocumentEdited() {
  }

  public EmbeddedDocumentEdited editUrl(URI editUrl) {
    this.editUrl = editUrl;
    return this;
  }

  /**
   * Get editUrl
   * @return editUrl
   */
  @javax.annotation.Nullable
  public URI getEditUrl() {
    return editUrl;
  }

  public void setEditUrl(URI editUrl) {
    this.editUrl = editUrl;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    EmbeddedDocumentEdited embeddedDocumentEdited = (EmbeddedDocumentEdited) o;
    return Objects.equals(this.editUrl, embeddedDocumentEdited.editUrl);
  }

  @Override
  public int hashCode() {
    return Objects.hash(editUrl);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class EmbeddedDocumentEdited {\n");
    sb.append("    editUrl: ").append(toIndentedString(editUrl)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (editUrl != null) {
        if (isFileTypeOrListOfFiles(editUrl)) {
            fileTypeFound = true;
        }

        if (editUrl.getClass().equals(java.io.File.class) ||
            editUrl.getClass().equals(Integer.class) ||
            editUrl.getClass().equals(String.class) ||
            editUrl.getClass().equals(java.net.URI.class)||
            editUrl.getClass().isEnum()) {
            map.put("editUrl", editUrl);
        } else if (isListOfFile(editUrl)) {
            for(int i = 0; i< getListSize(editUrl); i++) {
                map.put("editUrl", editUrl);
            }
        }
        else {
          map.put("editUrl", JSON.serialize(editUrl));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("editUrl");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to EmbeddedDocumentEdited
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!EmbeddedDocumentEdited.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in EmbeddedDocumentEdited is not found in the empty JSON string", EmbeddedDocumentEdited.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("editUrl") != null && !jsonObj.get("editUrl").isJsonNull()) && !jsonObj.get("editUrl").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `editUrl` to be a primitive type in the JSON string but got `%s`", jsonObj.get("editUrl").toString()));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!EmbeddedDocumentEdited.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'EmbeddedDocumentEdited' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<EmbeddedDocumentEdited> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(EmbeddedDocumentEdited.class));

       return (TypeAdapter<T>) new TypeAdapter<EmbeddedDocumentEdited>() {
           @Override
           public void write(JsonWriter out, EmbeddedDocumentEdited value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public EmbeddedDocumentEdited read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of EmbeddedDocumentEdited given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of EmbeddedDocumentEdited
   * @throws IOException if the JSON string is invalid with respect to EmbeddedDocumentEdited
   */
  public static EmbeddedDocumentEdited fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, EmbeddedDocumentEdited.class);
  }

  /**
   * Convert an instance of EmbeddedDocumentEdited to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

