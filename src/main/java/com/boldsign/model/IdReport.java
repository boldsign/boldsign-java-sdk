/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.boldsign.model.Error;
import com.boldsign.model.IdDocument;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.time.OffsetDateTime;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * IdReport
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class IdReport {
  public static final String SERIALIZED_NAME_VERIFICATION_RESULT = "verificationResult";
  @SerializedName(SERIALIZED_NAME_VERIFICATION_RESULT)
  private String verificationResult;

  public static final String SERIALIZED_NAME_VERIFIED_DATE = "verifiedDate";
  @SerializedName(SERIALIZED_NAME_VERIFIED_DATE)
  private OffsetDateTime verifiedDate;

  public static final String SERIALIZED_NAME_ERROR = "error";
  @SerializedName(SERIALIZED_NAME_ERROR)
  private Error error;

  public static final String SERIALIZED_NAME_DOCUMENT = "document";
  @SerializedName(SERIALIZED_NAME_DOCUMENT)
  private IdDocument document;

  public IdReport() {
  }

  public IdReport verificationResult(String verificationResult) {
    this.verificationResult = verificationResult;
    return this;
  }

  /**
   * Get verificationResult
   * @return verificationResult
   */
  @javax.annotation.Nullable
  public String getVerificationResult() {
    return verificationResult;
  }

  public void setVerificationResult(String verificationResult) {
    this.verificationResult = verificationResult;
  }


  public IdReport verifiedDate(OffsetDateTime verifiedDate) {
    this.verifiedDate = verifiedDate;
    return this;
  }

  /**
   * Get verifiedDate
   * @return verifiedDate
   */
  @javax.annotation.Nullable
  public OffsetDateTime getVerifiedDate() {
    return verifiedDate;
  }

  public void setVerifiedDate(OffsetDateTime verifiedDate) {
    this.verifiedDate = verifiedDate;
  }


  public IdReport error(Error error) {
    this.error = error;
    return this;
  }

  /**
   * Get error
   * @return error
   */
  @javax.annotation.Nullable
  public Error getError() {
    return error;
  }

  public void setError(Error error) {
    this.error = error;
  }


  public IdReport document(IdDocument document) {
    this.document = document;
    return this;
  }

  /**
   * Get document
   * @return document
   */
  @javax.annotation.Nullable
  public IdDocument getDocument() {
    return document;
  }

  public void setDocument(IdDocument document) {
    this.document = document;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    IdReport idReport = (IdReport) o;
    return Objects.equals(this.verificationResult, idReport.verificationResult) &&
        Objects.equals(this.verifiedDate, idReport.verifiedDate) &&
        Objects.equals(this.error, idReport.error) &&
        Objects.equals(this.document, idReport.document);
  }

  @Override
  public int hashCode() {
    return Objects.hash(verificationResult, verifiedDate, error, document);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class IdReport {\n");
    sb.append("    verificationResult: ").append(toIndentedString(verificationResult)).append("\n");
    sb.append("    verifiedDate: ").append(toIndentedString(verifiedDate)).append("\n");
    sb.append("    error: ").append(toIndentedString(error)).append("\n");
    sb.append("    document: ").append(toIndentedString(document)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (verificationResult != null) {
        if (isFileTypeOrListOfFiles(verificationResult)) {
            fileTypeFound = true;
        }

        if (verificationResult.getClass().equals(java.io.File.class) ||
            verificationResult.getClass().equals(Integer.class) ||
            verificationResult.getClass().equals(String.class) ||
            verificationResult.getClass().equals(java.net.URI.class)||
            verificationResult.getClass().isEnum()) {
            map.put("verificationResult", verificationResult);
        } else if (isListOfFile(verificationResult)) {
            for(int i = 0; i< getListSize(verificationResult); i++) {
                map.put("verificationResult", verificationResult);
            }
        }
        else {
          map.put("verificationResult", JSON.serialize(verificationResult));
        }
    }
    if (verifiedDate != null) {
        if (isFileTypeOrListOfFiles(verifiedDate)) {
            fileTypeFound = true;
        }

        if (verifiedDate.getClass().equals(java.io.File.class) ||
            verifiedDate.getClass().equals(Integer.class) ||
            verifiedDate.getClass().equals(String.class) ||
            verifiedDate.getClass().equals(java.net.URI.class)||
            verifiedDate.getClass().isEnum()) {
            map.put("verifiedDate", verifiedDate);
        } else if (isListOfFile(verifiedDate)) {
            for(int i = 0; i< getListSize(verifiedDate); i++) {
                map.put("verifiedDate", verifiedDate);
            }
        }
        else {
          map.put("verifiedDate", JSON.serialize(verifiedDate));
        }
    }
    if (error != null) {
        if (isFileTypeOrListOfFiles(error)) {
            fileTypeFound = true;
        }

        if (error.getClass().equals(java.io.File.class) ||
            error.getClass().equals(Integer.class) ||
            error.getClass().equals(String.class) ||
            error.getClass().equals(java.net.URI.class)||
            error.getClass().isEnum()) {
            map.put("error", error);
        } else if (isListOfFile(error)) {
            for(int i = 0; i< getListSize(error); i++) {
                map.put("error", error);
            }
        }
        else {
          map.put("error", JSON.serialize(error));
        }
    }
    if (document != null) {
        if (isFileTypeOrListOfFiles(document)) {
            fileTypeFound = true;
        }

        if (document.getClass().equals(java.io.File.class) ||
            document.getClass().equals(Integer.class) ||
            document.getClass().equals(String.class) ||
            document.getClass().equals(java.net.URI.class)||
            document.getClass().isEnum()) {
            map.put("document", document);
        } else if (isListOfFile(document)) {
            for(int i = 0; i< getListSize(document); i++) {
                map.put("document", document);
            }
        }
        else {
          map.put("document", JSON.serialize(document));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("verificationResult");
    openapiFields.add("verifiedDate");
    openapiFields.add("error");
    openapiFields.add("document");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to IdReport
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!IdReport.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in IdReport is not found in the empty JSON string", IdReport.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      if ((jsonObj.get("verificationResult") != null && !jsonObj.get("verificationResult").isJsonNull()) && !jsonObj.get("verificationResult").isJsonPrimitive()) {
        throw new IllegalArgumentException(String.format("Expected the field `verificationResult` to be a primitive type in the JSON string but got `%s`", jsonObj.get("verificationResult").toString()));
      }
      // validate the optional field `error`
      if (jsonObj.get("error") != null && !jsonObj.get("error").isJsonNull()) {
        Error.validateJsonElement(jsonObj.get("error"));
      }
      // validate the optional field `document`
      if (jsonObj.get("document") != null && !jsonObj.get("document").isJsonNull()) {
        IdDocument.validateJsonElement(jsonObj.get("document"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!IdReport.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'IdReport' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<IdReport> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(IdReport.class));

       return (TypeAdapter<T>) new TypeAdapter<IdReport>() {
           @Override
           public void write(JsonWriter out, IdReport value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public IdReport read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of IdReport given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of IdReport
   * @throws IOException if the JSON string is invalid with respect to IdReport
   */
  public static IdReport fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, IdReport.class);
  }

  /**
   * Convert an instance of IdReport to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

