/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * TextTagOffset
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class TextTagOffset {
  public static final String SERIALIZED_NAME_OFFSET_X = "offsetX";
  @SerializedName(SERIALIZED_NAME_OFFSET_X)
  private Double offsetX;

  public static final String SERIALIZED_NAME_OFFSET_Y = "offsetY";
  @SerializedName(SERIALIZED_NAME_OFFSET_Y)
  private Double offsetY;

  public TextTagOffset() {
  }

  public TextTagOffset offsetX(Double offsetX) {
    this.offsetX = offsetX;
    return this;
  }

  /**
   * Get offsetX
   * @return offsetX
   */
  @javax.annotation.Nullable
  public Double getOffsetX() {
    return offsetX;
  }

  public void setOffsetX(Double offsetX) {
    this.offsetX = offsetX;
  }


  public TextTagOffset offsetY(Double offsetY) {
    this.offsetY = offsetY;
    return this;
  }

  /**
   * Get offsetY
   * @return offsetY
   */
  @javax.annotation.Nullable
  public Double getOffsetY() {
    return offsetY;
  }

  public void setOffsetY(Double offsetY) {
    this.offsetY = offsetY;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    TextTagOffset textTagOffset = (TextTagOffset) o;
    return Objects.equals(this.offsetX, textTagOffset.offsetX) &&
        Objects.equals(this.offsetY, textTagOffset.offsetY);
  }

  @Override
  public int hashCode() {
    return Objects.hash(offsetX, offsetY);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class TextTagOffset {\n");
    sb.append("    offsetX: ").append(toIndentedString(offsetX)).append("\n");
    sb.append("    offsetY: ").append(toIndentedString(offsetY)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (offsetX != null) {
        if (isFileTypeOrListOfFiles(offsetX)) {
            fileTypeFound = true;
        }

        if (offsetX.getClass().equals(java.io.File.class) ||
            offsetX.getClass().equals(Integer.class) ||
            offsetX.getClass().equals(String.class) ||
            offsetX.getClass().equals(java.net.URI.class)||
            offsetX.getClass().isEnum()) {
            map.put("offsetX", offsetX);
        } else if (isListOfFile(offsetX)) {
            for(int i = 0; i< getListSize(offsetX); i++) {
                map.put("offsetX", offsetX);
            }
        }
        else {
          map.put("offsetX", JSON.serialize(offsetX));
        }
    }
    if (offsetY != null) {
        if (isFileTypeOrListOfFiles(offsetY)) {
            fileTypeFound = true;
        }

        if (offsetY.getClass().equals(java.io.File.class) ||
            offsetY.getClass().equals(Integer.class) ||
            offsetY.getClass().equals(String.class) ||
            offsetY.getClass().equals(java.net.URI.class)||
            offsetY.getClass().isEnum()) {
            map.put("offsetY", offsetY);
        } else if (isListOfFile(offsetY)) {
            for(int i = 0; i< getListSize(offsetY); i++) {
                map.put("offsetY", offsetY);
            }
        }
        else {
          map.put("offsetY", JSON.serialize(offsetY));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("offsetX");
    openapiFields.add("offsetY");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to TextTagOffset
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!TextTagOffset.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in TextTagOffset is not found in the empty JSON string", TextTagOffset.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!TextTagOffset.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'TextTagOffset' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<TextTagOffset> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(TextTagOffset.class));

       return (TypeAdapter<T>) new TypeAdapter<TextTagOffset>() {
           @Override
           public void write(JsonWriter out, TextTagOffset value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public TextTagOffset read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of TextTagOffset given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of TextTagOffset
   * @throws IOException if the JSON string is invalid with respect to TextTagOffset
   */
  public static TextTagOffset fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, TextTagOffset.class);
  }

  /**
   * Convert an instance of TextTagOffset to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

