/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.boldsign.model.DocumentEvent;
import com.boldsign.model.IWebhookData;
import com.boldsign.model.WebhookContext;
import com.boldsign.model.WebhookEventMetadata;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;
import java.lang.reflect.Method;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * WebhookEvent
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class WebhookEvent {
  public static final String SERIALIZED_NAME_EVENT = "event";
  @SerializedName(SERIALIZED_NAME_EVENT)
  private WebhookEventMetadata event;

  public static final String SERIALIZED_NAME_CONTEXT = "context";
  @SerializedName(SERIALIZED_NAME_CONTEXT)
  private WebhookContext context;

  public static final String SERIALIZED_NAME_DATA = "data";
  @SerializedName(SERIALIZED_NAME_DATA)
  private IWebhookData data;

  public static final String SERIALIZED_NAME_DOCUMENT = "document";
  @SerializedName(SERIALIZED_NAME_DOCUMENT)
  private DocumentEvent document;

  public WebhookEvent() {
  }

  public WebhookEvent event(WebhookEventMetadata event) {
    this.event = event;
    return this;
  }

  /**
   * Get event
   * @return event
   */
  @javax.annotation.Nullable
  public WebhookEventMetadata getEvent() {
    return event;
  }

  public void setEvent(WebhookEventMetadata event) {
    this.event = event;
  }


  public WebhookEvent context(WebhookContext context) {
    this.context = context;
    return this;
  }

  /**
   * Get context
   * @return context
   */
  @javax.annotation.Nullable
  public WebhookContext getContext() {
    return context;
  }

  public void setContext(WebhookContext context) {
    this.context = context;
  }


  public WebhookEvent data(IWebhookData data) {
    this.data = data;
    return this;
  }

  /**
   * Get data
   * @return data
   */
  @javax.annotation.Nullable
  public IWebhookData getData() {
    return data;
  }

  public void setData(IWebhookData data) {
    this.data = data;
  }


  public WebhookEvent document(DocumentEvent document) {
    this.document = document;
    return this;
  }

  /**
   * Get document
   * @return document
   */
  @javax.annotation.Nullable
  public DocumentEvent getDocument() {
    return document;
  }

  public void setDocument(DocumentEvent document) {
    this.document = document;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    WebhookEvent webhookEvent = (WebhookEvent) o;
    return Objects.equals(this.event, webhookEvent.event) &&
        Objects.equals(this.context, webhookEvent.context) &&
        Objects.equals(this.data, webhookEvent.data) &&
        Objects.equals(this.document, webhookEvent.document);
  }

  @Override
  public int hashCode() {
    return Objects.hash(event, context, data, document);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class WebhookEvent {\n");
    sb.append("    event: ").append(toIndentedString(event)).append("\n");
    sb.append("    context: ").append(toIndentedString(context)).append("\n");
    sb.append("    data: ").append(toIndentedString(data)).append("\n");
    sb.append("    document: ").append(toIndentedString(document)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (event != null) {
        if (isFileTypeOrListOfFiles(event)) {
            fileTypeFound = true;
        }

        if (event.getClass().equals(java.io.File.class) ||
            event.getClass().equals(Integer.class) ||
            event.getClass().equals(String.class) ||
            event.getClass().equals(java.net.URI.class)||
            event.getClass().isEnum()) {
            map.put("event", event);
        } else if (isListOfFile(event)) {
            for(int i = 0; i< getListSize(event); i++) {
                map.put("event", event);
            }
        }
        else {
          map.put("event", JSON.serialize(event));
        }
    }
    if (context != null) {
        if (isFileTypeOrListOfFiles(context)) {
            fileTypeFound = true;
        }

        if (context.getClass().equals(java.io.File.class) ||
            context.getClass().equals(Integer.class) ||
            context.getClass().equals(String.class) ||
            context.getClass().equals(java.net.URI.class)||
            context.getClass().isEnum()) {
            map.put("context", context);
        } else if (isListOfFile(context)) {
            for(int i = 0; i< getListSize(context); i++) {
                map.put("context", context);
            }
        }
        else {
          map.put("context", JSON.serialize(context));
        }
    }
    if (data != null) {
        if (isFileTypeOrListOfFiles(data)) {
            fileTypeFound = true;
        }

        if (data.getClass().equals(java.io.File.class) ||
            data.getClass().equals(Integer.class) ||
            data.getClass().equals(String.class) ||
            data.getClass().equals(java.net.URI.class)||
            data.getClass().isEnum()) {
            map.put("data", data);
        } else if (isListOfFile(data)) {
            for(int i = 0; i< getListSize(data); i++) {
                map.put("data", data);
            }
        }
        else {
          map.put("data", JSON.serialize(data));
        }
    }
    if (document != null) {
        if (isFileTypeOrListOfFiles(document)) {
            fileTypeFound = true;
        }

        if (document.getClass().equals(java.io.File.class) ||
            document.getClass().equals(Integer.class) ||
            document.getClass().equals(String.class) ||
            document.getClass().equals(java.net.URI.class)||
            document.getClass().isEnum()) {
            map.put("document", document);
        } else if (isListOfFile(document)) {
            for(int i = 0; i< getListSize(document); i++) {
                map.put("document", document);
            }
        }
        else {
          map.put("document", JSON.serialize(document));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("event");
    openapiFields.add("context");
    openapiFields.add("data");
    openapiFields.add("document");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to WebhookEvent
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!WebhookEvent.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in WebhookEvent is not found in the empty JSON string", WebhookEvent.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
      // validate the optional field `event`
      if (jsonObj.get("event") != null && !jsonObj.get("event").isJsonNull()) {
        WebhookEventMetadata.validateJsonElement(jsonObj.get("event"));
      }
      // validate the optional field `context`
      if (jsonObj.get("context") != null && !jsonObj.get("context").isJsonNull()) {
        WebhookContext.validateJsonElement(jsonObj.get("context"));
      }
      // validate the optional field `data`
      if (jsonObj.get("data") != null && !jsonObj.get("data").isJsonNull()) {
        IWebhookData.validateJsonElement(jsonObj.get("data"));
      }
      // validate the optional field `document`
      if (jsonObj.get("document") != null && !jsonObj.get("document").isJsonNull()) {
        DocumentEvent.validateJsonElement(jsonObj.get("document"));
      }
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!WebhookEvent.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'WebhookEvent' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<WebhookEvent> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(WebhookEvent.class));

       return (TypeAdapter<T>) new TypeAdapter<WebhookEvent>() {
           @Override
           public void write(JsonWriter out, WebhookEvent value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public WebhookEvent read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of WebhookEvent given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of WebhookEvent
   * @throws IOException if the JSON string is invalid with respect to WebhookEvent
   */
  public static WebhookEvent fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, WebhookEvent.class);
  }

  /**
   * Convert an instance of WebhookEvent to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

