/*
 * BoldSign API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 1
 * Contact: support@boldsign.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.boldsign.model;

import java.util.Objects;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;

import com.google.gson.Gson;
import com.google.gson.JsonArray;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;

import java.util.HashMap;
import java.util.HashSet;
import java.util.List;
import java.util.Map;
import java.util.Set;
import java.lang.reflect.Field;
import java.net.URI;

import com.boldsign.JSON;
import com.boldsign.ApiException;

/**
 * FormFieldPermission
 */
@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.8.0")
public class FormFieldPermission {
  public static final String SERIALIZED_NAME_CAN_ADD = "canAdd";
  @SerializedName(SERIALIZED_NAME_CAN_ADD)
  private Boolean canAdd;

  public static final String SERIALIZED_NAME_CAN_MODIFY = "canModify";
  @SerializedName(SERIALIZED_NAME_CAN_MODIFY)
  private Boolean canModify;

  public static final String SERIALIZED_NAME_CAN_MODIFY_DEFAULT_VALUE = "canModifyDefaultValue";
  @SerializedName(SERIALIZED_NAME_CAN_MODIFY_DEFAULT_VALUE)
  private Boolean canModifyDefaultValue;

  public FormFieldPermission() {
  }

  public FormFieldPermission canAdd(Boolean canAdd) {
    this.canAdd = canAdd;
    return this;
  }

  /**
   * Get canAdd
   * @return canAdd
   */
  @javax.annotation.Nullable
  public Boolean getCanAdd() {
    return canAdd;
  }

  public void setCanAdd(Boolean canAdd) {
    this.canAdd = canAdd;
  }


  public FormFieldPermission canModify(Boolean canModify) {
    this.canModify = canModify;
    return this;
  }

  /**
   * Get canModify
   * @return canModify
   */
  @javax.annotation.Nullable
  public Boolean getCanModify() {
    return canModify;
  }

  public void setCanModify(Boolean canModify) {
    this.canModify = canModify;
  }


  public FormFieldPermission canModifyDefaultValue(Boolean canModifyDefaultValue) {
    this.canModifyDefaultValue = canModifyDefaultValue;
    return this;
  }

  /**
   * Get canModifyDefaultValue
   * @return canModifyDefaultValue
   */
  @javax.annotation.Nullable
  public Boolean getCanModifyDefaultValue() {
    return canModifyDefaultValue;
  }

  public void setCanModifyDefaultValue(Boolean canModifyDefaultValue) {
    this.canModifyDefaultValue = canModifyDefaultValue;
  }



  @Override
  public boolean equals(Object o) {
    if (this == o) {
      return true;
    }
    if (o == null || getClass() != o.getClass()) {
      return false;
    }
    FormFieldPermission formFieldPermission = (FormFieldPermission) o;
    return Objects.equals(this.canAdd, formFieldPermission.canAdd) &&
        Objects.equals(this.canModify, formFieldPermission.canModify) &&
        Objects.equals(this.canModifyDefaultValue, formFieldPermission.canModifyDefaultValue);
  }

  @Override
  public int hashCode() {
    return Objects.hash(canAdd, canModify, canModifyDefaultValue);
  }

  @Override
  public String toString() {
    StringBuilder sb = new StringBuilder();
    sb.append("class FormFieldPermission {\n");
    sb.append("    canAdd: ").append(toIndentedString(canAdd)).append("\n");
    sb.append("    canModify: ").append(toIndentedString(canModify)).append("\n");
    sb.append("    canModifyDefaultValue: ").append(toIndentedString(canModifyDefaultValue)).append("\n");
    sb.append("}");
    return sb.toString();
  }
  public Map<String, Object> createFormData() throws ApiException {
    Map<String, Object> map = new HashMap<>();
    boolean fileTypeFound = false;
    try {
    if (canAdd != null) {
        if (isFileTypeOrListOfFiles(canAdd)) {
            fileTypeFound = true;
        }

        if (canAdd.getClass().equals(java.io.File.class) ||
            canAdd.getClass().equals(Integer.class) ||
            canAdd.getClass().equals(String.class) ||
            canAdd.getClass().equals(java.net.URI.class)||
            canAdd.getClass().isEnum()) {
            map.put("canAdd", canAdd);
        } else if (isListOfFile(canAdd)) {
            for(int i = 0; i< getListSize(canAdd); i++) {
                map.put("canAdd", canAdd);
            }
        }
        else {
          map.put("canAdd", JSON.serialize(canAdd));
        }
    }
    if (canModify != null) {
        if (isFileTypeOrListOfFiles(canModify)) {
            fileTypeFound = true;
        }

        if (canModify.getClass().equals(java.io.File.class) ||
            canModify.getClass().equals(Integer.class) ||
            canModify.getClass().equals(String.class) ||
            canModify.getClass().equals(java.net.URI.class)||
            canModify.getClass().isEnum()) {
            map.put("canModify", canModify);
        } else if (isListOfFile(canModify)) {
            for(int i = 0; i< getListSize(canModify); i++) {
                map.put("canModify", canModify);
            }
        }
        else {
          map.put("canModify", JSON.serialize(canModify));
        }
    }
    if (canModifyDefaultValue != null) {
        if (isFileTypeOrListOfFiles(canModifyDefaultValue)) {
            fileTypeFound = true;
        }

        if (canModifyDefaultValue.getClass().equals(java.io.File.class) ||
            canModifyDefaultValue.getClass().equals(Integer.class) ||
            canModifyDefaultValue.getClass().equals(String.class) ||
            canModifyDefaultValue.getClass().equals(java.net.URI.class)||
            canModifyDefaultValue.getClass().isEnum()) {
            map.put("canModifyDefaultValue", canModifyDefaultValue);
        } else if (isListOfFile(canModifyDefaultValue)) {
            for(int i = 0; i< getListSize(canModifyDefaultValue); i++) {
                map.put("canModifyDefaultValue", canModifyDefaultValue);
            }
        }
        else {
          map.put("canModifyDefaultValue", JSON.serialize(canModifyDefaultValue));
        }
    }
    } catch (Exception e) {
        throw new ApiException(e);
    }
    return fileTypeFound ? map : new HashMap<>();
  }

  private boolean isFileTypeOrListOfFiles(Object obj) throws Exception {
    return obj.getClass().equals(java.io.File.class) || isListOfFile(obj);
  }

  private boolean isListOfFile(Object obj) throws Exception {
      return obj instanceof java.util.List && !isListEmpty(obj) && getFromList(obj, 0) instanceof java.io.File;
  }

  private boolean isListEmpty(Object obj) throws Exception {
    return (boolean) Class.forName(java.util.List.class.getName()).getMethod("isEmpty").invoke(obj);
  }

  private Object getFromList(Object obj, int index) throws Exception {
    return Class.forName(java.util.List.class.getName()).getMethod("get", int.class).invoke(obj, index);
  }

  private int getListSize(Object obj) throws Exception {
    return (int) Class.forName(java.util.List.class.getName()).getMethod("size").invoke(obj);
  }


  /**
   * Convert the given object to string with each line indented by 4 spaces
   * (except the first line).
   */
  private String toIndentedString(Object o) {
    if (o == null) {
      return "null";
    }
    return o.toString().replace("\n", "\n    ");
  }


  public static HashSet<String> openapiFields;
  public static HashSet<String> openapiRequiredFields;

  static {
    // a set of all properties/fields (JSON key names)
    openapiFields = new HashSet<String>();
    openapiFields.add("canAdd");
    openapiFields.add("canModify");
    openapiFields.add("canModifyDefaultValue");

    // a set of required properties/fields (JSON key names)
    openapiRequiredFields = new HashSet<String>();
  }

  /**
   * Validates the JSON Element and throws an exception if issues found
   *
   * @param jsonElement JSON Element
   * @throws IOException if the JSON Element is invalid with respect to FormFieldPermission
   */
  public static void validateJsonElement(JsonElement jsonElement) throws IOException {
      if (jsonElement == null) {
        if (!FormFieldPermission.openapiRequiredFields.isEmpty()) { // has required fields but JSON element is null
          throw new IllegalArgumentException(String.format("The required field(s) %s in FormFieldPermission is not found in the empty JSON string", FormFieldPermission.openapiRequiredFields.toString()));
        }
      }
	  //Ignored additional properties from response json. 
        JsonObject jsonObj = jsonElement.getAsJsonObject();
  }

  public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
    @SuppressWarnings("unchecked")
    @Override
    public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
       if (!FormFieldPermission.class.isAssignableFrom(type.getRawType())) {
         return null; // this class only serializes 'FormFieldPermission' and its subtypes
       }
       final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
       final TypeAdapter<FormFieldPermission> thisAdapter
                        = gson.getDelegateAdapter(this, TypeToken.get(FormFieldPermission.class));

       return (TypeAdapter<T>) new TypeAdapter<FormFieldPermission>() {
           @Override
           public void write(JsonWriter out, FormFieldPermission value) throws IOException {
             JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
             elementAdapter.write(out, obj);
           }

           @Override
           public FormFieldPermission read(JsonReader in) throws IOException {
             JsonElement jsonElement = elementAdapter.read(in);
             validateJsonElement(jsonElement);
             return thisAdapter.fromJsonTree(jsonElement);
           }

       }.nullSafe();
    }
  }

  /**
   * Create an instance of FormFieldPermission given an JSON string
   *
   * @param jsonString JSON string
   * @return An instance of FormFieldPermission
   * @throws IOException if the JSON string is invalid with respect to FormFieldPermission
   */
  public static FormFieldPermission fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, FormFieldPermission.class);
  }

  /**
   * Convert an instance of FormFieldPermission to an JSON string
   *
   * @return JSON string
   */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

